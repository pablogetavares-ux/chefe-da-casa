/** Re-export for backward compatibility — limits live in billing/plan-limits. */
export {
  assertAiGenerationAllowed,
  getAiUsageSummary,
  type AiUsageSummary,
} from "@/lib/billing/plan-limits";
